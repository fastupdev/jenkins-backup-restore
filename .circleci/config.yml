version: 2.1

jobs:
  build:
    build:
    docker: 
      - image: python:latest
    steps:
      - checkout
      - run:
          name: python-test
          command:
            python setup.py install --user && \
            jenkins-backup-restore-cli --help
